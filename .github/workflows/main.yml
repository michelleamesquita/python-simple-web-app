on:
  push:
    branches:
    - main

jobs:
  node-docker:
    runs-on: ubuntu-latest
    container:
      image: michelleamesquita/sast:latest
    steps:
         - name: Pull docker
           uses: docker://michelleamesquita/sast:latest
           
         - name: Start SAST
           run: docker run -v $PWD/:/app michelleamesquita/sast:latest -d=./app -l=python
#            uses: docker://michelleamesquita/sast:latest
#            with:
#              options: -v $PWD/:/app
#              args: -d=./app/ -l=python
             
             
