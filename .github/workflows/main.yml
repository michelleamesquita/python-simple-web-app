
on:
  push:
    branches:
    - main

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: SAST Validator
    steps:
      - uses: michelleamesquita/sast-validator@v2
        with:
          path: ./app/
          language: python
