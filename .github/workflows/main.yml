on:
  push:
    branches:
    - main

jobs:
  node-docker:
    runs-on: ubuntu-latest
    container:
      image: michelleamesquita/sast:latest
    steps:
         - name: Start SAST
           uses: docker://michelleamesquita/sast:latest
           with:
             args: -v $PWD/:/app sast-validator -d=./app/ -l=python
