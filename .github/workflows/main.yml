
on:
  push:
    branches:
    - main

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: SAST Validator
    steps:
      - uses: michelleamesquita/sast-validator@v8
        with:
          path: ./app/
          language: python
