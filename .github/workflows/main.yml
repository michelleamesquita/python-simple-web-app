
on:
  push:
    branches:
    - main

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: SAST Validator
    steps:
      - uses: actions/checkout@v1
      - uses: michelleamesquita/sast-validator@v7
        with:
          path: ./app/
          language: python
